export enum AuthStoreActions {
    SignIn = '[Authentication] SIGN_IN',
    SignOut = '[Authentication] SIGN_OUT',
    SetAttributes = '[Authentication] SET_ATTRIBUTES',
    InitializeState = '[Authentication] AUTH_INIT_STATE'
}
